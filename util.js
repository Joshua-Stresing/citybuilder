export function makeStatsString(raceNum, worldNum, terrainNum) {
    return `You have chosen races ${raceNum} times, ${worldNum} different worlds, and ${terrainNum} different terrains. And you came up with the caption(s):`;
}